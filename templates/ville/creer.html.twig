{% extends 'base.html.twig' %}

{% block title %}{% endblock %}

{% block body %}
    <h1>Ajout d'une ville</h1>
<div>
    {{ form_start(villeForm) }}
    {{ form_widget(villeForm) }}
    <button class="btn">Ajouter une ville</button>
    <a href="{{ path('lieu_creer') }}" class="btn">Retour</a>
    {{ form_end(villeForm) }}
</div>
{% endblock %}
