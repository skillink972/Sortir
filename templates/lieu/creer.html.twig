{% extends 'base.html.twig' %}

{% block title %}{% endblock %}

{% block body %}
    <h1>Ajout d'un lieu</h1>
    <div>
        {{ form_start(lieuForm) }}
        {{ form_widget(lieuForm) }}
        <a href="{{ path('ville_creer') }}" class="btn">Ajouter une ville</a> <br>
        <button class="btn">Ajouter un lieu</button>
        <a href="{{ path('sortie_creer') }}" class="btn">Retour</a>
        {{ form_end(lieuForm) }}
    </div>
{% endblock %}

