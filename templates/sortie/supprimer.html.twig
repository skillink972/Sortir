{% extends 'base.html.twig' %}

{% block title %}Supprimer sortie{% endblock %}

{% block body %}
<div class="example-wrapper">
    <h3>Suppresion de la sortie {{ sortie.nom }} ({{ sortie.etat.libelle }})</h3>
</div>
    {{ form_start(motifSuppressionForm) }}
    {{ form_widget(motifSuppressionForm) }}
    <button class="btn">Supprimer</button>
    {{ form_end(motifSuppressionForm) }}

{% endblock %}